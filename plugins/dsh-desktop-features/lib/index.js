/** Host half of the dsh-desktop-features group: all behavior lives in the browser bundle. */
const name = "dsh-desktop-features";
const inject = [];
function apply(ctx) {}
export { apply, inject, name };
